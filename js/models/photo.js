//Photo Model
//Manage a single photo

define([
  'underscore', 
  'backbone'
  ], function(_, Backbone){
    
  var Photo = Backbone.Model.extend({
  	defaults: {
    },

    initialize: function() {
    }

  });
  return Photo;
});